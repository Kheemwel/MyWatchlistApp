<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.MyWatchlistApp" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.MyWatchlistApp.SplashScreen" parent="Theme.SplashScreen">
        <!-- Set the splash screen background, animated icon, and animation
        duration. -->
        <item name="windowSplashScreenBackground">@color/white</item>
        <!-- Use windowSplashScreenAnimatedIcon to add a drawable or an animated
             drawable. One of these is required. -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/app_icon</item>
        <!-- Set the theme of the Activity that directly follows your splash
        screen. This is required. -->
        <item name="postSplashScreenTheme">@style/Theme.MyWatchlistApp</item>
    </style>
</resources>